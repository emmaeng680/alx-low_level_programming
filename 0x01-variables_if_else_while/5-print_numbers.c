#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print last digit of n with conditions'
 * Return: Always 0 (Success)
 */
int main(void)
{
int zero;

for (zero = 0 ; zero <= 9 ; zero++)
{
printf("%d", zero);

}
printf("\n");

return (0);
}

