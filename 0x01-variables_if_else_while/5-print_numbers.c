#include <stdio.h>
/**
 * main - Entry point
 * Description: 'prints 0 to 9'
 * Return: Always 0 (Success)
 */
int main(void)
{
int zero;

for (zero = 0 ; zero <= 9 ; zero++)
{
printf("%d", zero);

}
printf("\n");

return (0);
}

